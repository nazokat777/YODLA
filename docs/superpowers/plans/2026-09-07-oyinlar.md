# O'yinlar va dofamin — reja

Spec: `docs/superpowers/specs/2026-09-07-oyinlar-design.md`

Har vazifa: test → qizil → kod → yashil → commit; test o'zi ushlashi
kerak bo'lgan xatoni ushlashi isbotlanadi. `npm run build` majburiy.

- [ ] **1. So'z kuchi + omadli karta (sof mantiq)**
  `core/srs/strength.ts`: `wordStrength(card)` → 0..3 va nomlari.
  `core/games/luck.ts`: `isLucky(random)` 8%, `LUCKY_MULTIPLIER = 2`.

- [ ] **2. `ProfileRecord.gameBests` + repo**
  `gameBests?: Partial<Record<GameId, number>>`; `saveGameBest(id, score)`
  faqat rekord yangi bo'lsa yozadi va yangilanganini qaytaradi.

- [ ] **3. Vaqtga qarshi poyga — mantiq**
  `core/games/speed.ts`: `SPEED_SECONDS = 60`, holat mashinasi
  (`start/answer/tick`), ochko.

- [ ] **4. Vaqtga qarshi — ekran**
  `/games/speed`: taymer, savol, ochko; yakunda rekord.
  SM-2: to'g'ri 4 / xato 2; `typeStats` yoziladi.

- [ ] **5. Xotira o'yini**
  `core/games/memory.ts`: taxta qurish, ochish, juftlash.
  `/games/memory` ekrani.

- [ ] **6. To'g'rimi yoki xato?**
  `core/games/truefalse.ts`: juft yaratish (yarmi almashtirilgan).
  `/games/truefalse`. O'ZLASHTIRISHGA hisobga o'tmaydi.

- [ ] **7. Kunlik chaqiriq**
  `core/games/challenge.ts`: sana urug'idan barqaror vazifa,
  progress, bonus.

- [ ] **8. O'yinlar sahifasi va bosh ekran kartasi**
  `/games` — to'rt o'yin, rekordlar, kunlik chaqiriq.

- [ ] **9. Dofamin qatlami mashqlarda**
  Omadli karta (javobdan oldin e'lon) va so'z kuchi indikatori.

- [ ] **10. Tekshiruv:** testlar, lint, build, brauzerda har o'yin,
  commit, push.
