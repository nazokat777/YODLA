/// <reference types="vite/client" />

/**
 * Build belgisi — `vite.config.ts` da `define` orqali kiritiladi.
 *
 * Lug'at faqat yangi build bilan o'zgaradi, shuning uchun bu qiymat
 * "kontent eskirdimi" degan savolga lug'atni YUKLAMASDAN javob beradi.
 */
declare const __DECK_BUILD_ID__: string
